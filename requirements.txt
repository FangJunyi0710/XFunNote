# ===================== 核心依赖 =====================

typer               # 命令行工具（cli.py）
python-dotenv       # 管理环境变量（config.py）
future-uuid         # uuid7 生成（plan.py）
langchain>=0.3.0    # AI Agent 框架
langchain-openai>=0.2.0  # OpenAI/DeepSeek 兼容封装
langchain-core>=0.3.0    # LangChain 核心抽象
langgraph>=0.3.0    # LangGraph 有状态 Agent 编排

# ===================== 测试依赖 =====================

pytest              # 测试框架
pytest-cov          # 测试覆盖率

# ===================== 规划中（暂未使用） =====================

# fastapi           # 后端 API（backend/main.py 尚未实现）
# uvicorn[standard] # ASGI 服务器（配合 FastAPI）
# streamlit         # 前端界面（frontend/app.py 尚未实现）
# httpx             # HTTP 客户端（后端完成后使用）
